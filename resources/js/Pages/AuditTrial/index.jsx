import React from "react";
import Layout from "../../Components/Layout";

const index = () => {
    return <div>Audit Trial</div>;
};

index.layout = (page) => <Layout children={page} />;
export default index;
